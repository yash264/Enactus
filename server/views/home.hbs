<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Enactus NIT Allahabad</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            padding: 8px 12px;
            border: 1px solid #ddd;
            text-align: left;
        }

        th {
            background-color: #f5a623;
            color: white;
        }

        a {
            color: #1d4ed8;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <h1>Enactus NIT Allahabad</h1>
    <h2>All Orders List -</h2>

    <form method="GET" action="/">
        <input type="text" name="q" placeholder="Search by name or email" value="{{query}}"
            style="padding: 6px; width: 250px;" />
        <button type="submit" style="padding: 6px;">Search</button>
    </form>

    {{#if orders.length}}
    <table border="1" cellpadding="10" cellspacing="0" style="margin-top: 20px;">
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Mobile No.</th>
                <th>Date</th>
                <th>Invoice</th>
            </tr>
        </thead>
        <tbody>
            {{#each orders}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.email}}</td>
                <td>{{this.phoneNo}}</td>
                <td>{{formatDate this.createdAt}}</td>
                <td><a href="/invoice/{{this._id}}">View</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{else}}
    <p>No orders found.</p>
    {{/if}}

</body>

</html>